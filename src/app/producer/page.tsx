import type { Metadata } from "next"
import { ProducerDeskWithLaunch } from "@/components/producer-desk-with-launch"

export const metadata: Metadata = { title: "Producer Desk" }
export default function ProducerPage() { return <ProducerDeskWithLaunch /> }
